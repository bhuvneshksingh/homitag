import { defineMessages } from 'react-intl';

export const scope = 'Common.Cards.MainPageCard';

export default defineMessages({
  noItem: {
    id: `${scope}.noItem`,
    defaultMessage: 'Sorry. No item to show',
  },
});
